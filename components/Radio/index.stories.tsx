import React from 'react'
import { text, select, boolean } from '@storybook/addon-knobs'
import Radio from '.'

export default {
  title: 'Radio',
}

export const RadioStory = () => {
  return <Radio />
}
